import { db } from "../config/firebaseConfig";
import { useState } from "react";
import {
  collection,
  addDoc,
  getDocs,
  updateDoc,
  doc,
  deleteDoc,
  onSnapshot,
} from "firebase/firestore";

const ReadDonutData = () => {
    return (
        <div>
            
        </div>
    )
}